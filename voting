#include <stdio.h>

int main()
{
    char name[10];
    printf("Name");
    fgets(name, 10, stdin);
    
    int age;
    printf("Age:");
    scanf("%d",&age);
    
    if (age<18)
        printf("You can't vote");
        
    else
        printf("You can vote");

    return 0;
}
